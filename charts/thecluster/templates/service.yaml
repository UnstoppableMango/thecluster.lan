apiVersion: v1
kind: Service
metadata:
  name: {{ include "thecluster.fullname" . }}
  labels:
    {{- include "thecluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "thecluster.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
